import 'package:flutter/material.dart';


  const themeColorSubtitle = Colors.grey;
  const themeColorRadio = Colors.black;
  const backgroundColor = Color.fromRGBO(19, 28, 33,1);
  //const textColor = Color.fromRGBO(241, 241, 242, 1);
  const lightBlack = Color.fromRGBO(33, 33, 33,1);
  const textColorChatBox = Colors.black;
  const textColorMyMessage = Colors.white;
  const textColorYourMessage = Colors.black;
  const appBarColor =  Colors.black;
  const webAppBarColor = Color.fromRGBO(42,47,50,1);
  const messageColor = Color.fromRGBO(33,66,171,1);
  const senderMessageColor = Colors.white60;
 // const messageColor =Color.fromRGBO(0, 63, 92, 1);
 // const senderMessageColor = Color.fromRGBO(37, 45, 49, 1);
//const tabColor = Color.fromRGBO(0, 167, 131, 1);
  const tabColor = Color.fromRGBO(241, 241, 242, 1);
  const searchBarColor = Color.fromRGBO(50, 55, 57, 1);
  const dividerColor = Color.fromRGBO(37, 45, 50, 1);
  const chatBarMessage = Color.fromRGBO(30, 36, 40, 1);
  //const mobileChatBoxColor = Color.fromRGBO(31, 44, 52, 1);
  const mobileChatBoxColor = Colors.white60;
  const themeButtonColor = Color.fromRGBO(246, 95, 90, 1);

